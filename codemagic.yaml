workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 60
    environment:
      flutter: stable
    scripts:
      - name: Build APK
        script: |
          flutter create --platforms android .
          flutter build apk --release
    artifacts:
      - build/**/outputs/**/*.apk
